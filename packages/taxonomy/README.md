# Taxonomy Package

Planned shared package for typed access to `report-templates/input-taxonomy.yaml`.

The taxonomy remains canonical in `report-templates/` until the API and web app need a compiled shared package.
