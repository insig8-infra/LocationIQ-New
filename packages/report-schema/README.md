# Report Schema Package

Planned shared package for typed access to `report-templates/output-schema.json`.

The schema remains canonical in `report-templates/` until the first generator/renderer integration needs a compiled package.
